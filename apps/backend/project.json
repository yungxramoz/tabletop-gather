{
  "name": "backend",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "prefix": "tabletop-gather",
  "sourceRoot": "apps/backend/src",
  "tags": [],
  "targets": {
    "deps": {
      "command": "apps\\backend\\mvnw.cmd dependency:resolve"
    },
    "build": {
      "command": "apps\\backend\\mvnw.cmd clean package -DskipTests",
      "dependsOn": ["deps"]
    },
    "serve": {
      "command": "apps\\backend\\mvnw.cmd spring-boot:run",
      "dependsOn": ["build"]
    },
    "test": {
      "command": "apps\\backend\\mvnw.cmd test",
      "dependsOn": ["build"]
    },
    "docker-build": {
      "command": "docker build --tag tg-java-backend apps/backend"
    },
    "docker-serve": {
      "command": "docker compose -f ./apps/backend/docker-compose.yml up --no-build",
      "dependsOn": ["docker-build"]
    }
  }
}
